# Ship Feature Workflow

1. **Build**: Run `npm run build` and check for errors.
2. **Lint**: Check for "Unknown Action" or unused vars.
3. **Verify**: Ask the user to verify the feature locally.
4. **Deploy**: ONLY after approval, run `firebase deploy`.
