# OlyBars Environment Configuration Template
# =========================================
# Copy this to .env.local for local development.
# Prod values live in GCP Secret Manager.

# --- INFRASTRUCTURE ---
NODE_ENV=development
PORT=3001
GOOGLE_CLOUD_PROJECT=ama-ecosystem-prod

# --- SECURITY ---
# Internal token for diagnostic /api/health/artie (must match X-Internal-Token header)
INTERNAL_HEALTH_TOKEN=your_local_secret_token

# --- BACKEND SECRETS (Private - NEVER use VITE_ prefix) ---
# Gemini API Key (Required for local/Antigravity; ADC used in Cloud Run)
GOOGLE_GENAI_API_KEY=your_gemini_api_key

# Google Maps Backend Key (For Places/Geocoding services)
GOOGLE_BACKEND_KEY=your_google_backend_api_key

# --- FRONTEND ASSETS (Public - MUST use VITE_ prefix) ---
# Google Maps Browser Key (Restricted to HTTP Referrer)
VITE_GOOGLE_BROWSER_KEY=your_google_browser_api_key

# reCAPTCHA / App Check Site Key (Public)
VITE_APP_CHECK_KEY=6LcA_6oqAAAAAHzb_1uR_p8Zk9zK9jZk9zK9jZk9

# URLs
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3001/api
